'use client'
import {useState} from 'react'; import {useRouter} from 'next/navigation'; import {supabaseBrowser} from '@/lib/supabase-browser'
export default function Login(){const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [signup,setSignup]=useState(false);const [msg,setMsg]=useState('');const router=useRouter();async function go(){setMsg('处理中...');const s=supabaseBrowser();const r=signup?await s.auth.signUp({email,password}):await s.auth.signInWithPassword({email,password});if(r.error)return setMsg(r.error.message);if(signup){setMsg('注册成功，请检查邮箱验证（若项目关闭邮箱确认会直接登录）。')}else router.push('/dashboard')}return <main className="auth"><div className="authbox"><div className="tag">餐谋AI</div><h1>{signup?'创建老板账号':'欢迎回来'}</h1><input placeholder="邮箱" value={email} onChange={e=>setEmail(e.target.value)}/><input placeholder="密码（至少6位）" type="password" value={password} onChange={e=>setPassword(e.target.value)}/><button className="btn primary full" onClick={go}>{signup?'注册':'登录'}</button><button className="linkbtn" onClick={()=>setSignup(!signup)}>{signup?'已有账号？登录':'没有账号？免费注册'}</button>{msg&&<p className="notice">{msg}</p>}</div></main>}
