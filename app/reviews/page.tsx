'use client'
import {useState} from 'react';import Link from 'next/link'
export default function Reviews(){const [content,setContent]=useState('');const [loading,setLoading]=useState(false);const [r,setR]=useState<any>();const [err,setErr]=useState('');async function analyze(){setErr('');setR(undefined);setLoading(true);const x=await fetch('/api/analyze',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({content})});const j=await x.json();setLoading(false);if(!x.ok){setErr(j.error||'分析失败');return}setR(j.result)}return <main className="app"><header><div><Link className="back" href="/dashboard">← 工作台</Link><h1>AI评价分析</h1></div><Link className="btn" href="/pricing">升级PRO</Link></header><div className="split"><section className="card"><h2>粘贴顾客评价</h2><textarea value={content} onChange={e=>setContent(e.target.value)} placeholder="例如：饭送过来都凉了，分量还少，差评！"/><button className="btn primary full" disabled={loading} onClick={analyze}>{loading?'AI正在分析…':'开始分析'}</button>{err&&<p className="error">{err}</p>}</section>{r&&<section className="card result"><h2>AI诊断结果</h2><div className="pill">{r.sentiment} · {r.problemType} · 紧急度：{r.urgency}</div><h3>问题总结</h3><p>{r.problemSummary}</p><h3>公开回复</h3><div className="quote">{r.publicReply}</div><h3>顾客挽回方案</h3><p>{r.recoveryAction}</p><h3>老板应该做什么</h3><p>{r.managerAdvice}</p></section>}</div></main>}
